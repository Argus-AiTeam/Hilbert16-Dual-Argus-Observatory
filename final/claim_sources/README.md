# Claim-bearing source files

No upstream claim-bearing source files are bundled here. The reviewed paper and audits identify them by `math1/` and `math2/` path labels, byte sizes, and SHA-256 hashes in `SOURCE_MANIFEST.json`. Cached papers, virtual environments, private paths, and unreviewed exploratory outputs are intentionally excluded from publication.
